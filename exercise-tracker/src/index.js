import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import App from './App'; //where we will create our file -> the starting point
//import reportWebVitals from './reportWebVitals';

ReactDOM.render( <
    App / > ,
    document.getElementById('root') //this is where the div in index.html is being called
);